<%= form_tag :url => {:action => :create},
															:remote => true,
															:html => {:class => "form-horizontal"} do |f| %>
	  <%= text_area_tag(:size => "24x6") %>
	  <%= submit_tag("Send", :class => "btn btn-primary btn-small") %>
<% end %>